{
  "name": "ActivitySet",
  "type": "object",
  "properties": {
    "content_key": {
      "type": "string",
      "description": "The id this set has in content/activity-sets.md. A set is a hand-picked list that nothing else reproduces, so the file is its backup; the id is what lets a sync tell a renamed set from a new one."
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string",
      "description": "What this set is for, shown wherever a facilitator picks a preset. A name alone does not say what \"Brief\" covers or when to reach for it."
    },
    "activity_ids": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "sort_order": {
      "type": "number"
    },
    "active": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "name"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {},
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}